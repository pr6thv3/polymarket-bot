# Research-safe environment template
#
# The supported public workflow is read-only and does not require wallet keys,
# exchange trading credentials, deposits, or authenticated trading endpoints.

# Optional: only needed if you work on deferred AI/news research.
OPENAI_API_KEY=

# Optional: only needed if you work on notification demos.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: paper/accounting-only value used by some legacy tests/tools.
STARTING_CAPITAL=1000
